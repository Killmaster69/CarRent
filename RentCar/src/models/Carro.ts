export interface Carro {
  _id: string;
  matricula: string;
  marca: string;
  modelo: string;
  color: string;
  precio: string;
  descripcion: string;
  estado: string;
  imagen: string; // Imagen en base64 o URL
}
